apiVersion: v1
kind: Service
metadata:
  name: coffee
  namespace: '${ parameters.namespace }'
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: coffee
